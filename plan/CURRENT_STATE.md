# Estado da implementação

Este documento registra onde a implementação do plano está hoje, e cada unidade de trabalho o
atualiza. O plano, com as decisões, as regras e as etapas, está em [`PLAN.md`](PLAN.md); o que as
provas de conceito, as suítes e os probes mostraram, com a data de cada medição e cada execução, em
[`POC.md`](POC.md); as pendências e as decisões em aberto, em
[`OPEN_QUESTIONS.md`](OPEN_QUESTIONS.md); o histórico das mudanças, no git. O estado descrito aqui é
o de 2026-09-24.

## Situação de cada etapa

O pacote tem os módulos das etapas 1 a 8 e os subcomandos da etapa 9, e a etapa 10 está
planejada; a ordem do trabalho está em [`PLAN.md`](PLAN.md). Cada arquivo de etapa fixa as
primitivas, a estratégia de implementação, os pré-requisitos e as pós-condições, os testes por
caso e as decisões pendentes.

| Etapa | Situação |
| --- | --- |
| [0. Prova de conceito na AWS](PLAN-STAGE-0.md) | Concluída em 2026-09-21: os itens de S3 verificados, os cinco probes rodados no ambiente alvo e a suíte `-m redshift` limpa duas vezes seguidas lá, às 13:35 e às 13:39 UTC. A leitura `RS-8` do probe, em 2026-09-23, achou `svv_table_info` negada ao papel depois do `USE` (42501). |
| [1. `schema`](PLAN-STAGE-1.md) | Concluída em 2026-09-21: o modelo cliente, `serialize_db.schema`, `ContractError` e `serialize-db schema write\|check`, com os 36 arquivos de esquema do modelo cliente versionados. A revisão de 2026-09-22 corrigiu três defeitos do `cast`: o texto em `large_string`, `string_view` e dicionário sem a medida de bytes, o tipo sem conversão fora de `ContractError` e o inteiro em `Numeric` só na precisão 18. O `cast` recusa o `timestamp` com fuso numa coluna `DateTime` sem fuso e o inverso, e o documento JSON acima de 65.535 bytes (decisões do usuário de 2026-09-23). O `cast` aceita o `Double` não finito (decisão do usuário de 2026-09-23), e o que ele faz nas estatísticas do Delta está na [issue #59](https://github.com/felipenoris/serialize-db/issues/59). |
| [2. `sql`](PLAN-STAGE-2.md) | Concluída em 2026-09-22: `serialize_db.sql`, `SqlError` e `serialize-db sql write\|check`, com os quatro statements do pipeline fictício e os oito arquivos de texto SQL versionados. O caminho padrão é o statement Core compilado pelo motor; o texto versionado é a opção de migração para fora do SQLAlchemy. Nenhuma decisão espera o usuário. |
| [3. `storage` e `delta`](PLAN-STAGE-3.md) | Implementada em 2026-09-23 em pasta local: `serialize_db.storage` (`Storage` sobre `pyarrow.fs`, a escrita condicional, as opções do delta-rs sem credencial, a conexão do DuckDB com as extensões da pasta configurada e `prepare_environment`), `serialize_db.delta` (a camada de tabela inteira, com as conferências do registro e a releitura) e as exceções da etapa em `serialize_db.errors`; a regra da partição entrou em `serialize_db.schema` (`PARTITION_VALUE`, `check_partition_value`). As colunas `Double` com valor não finito saem sem mínimo e máximo no rodapé e no log pela lista `columns_without_min_max` ([issue #59](https://github.com/felipenoris/serialize-db/issues/59)), e `rewrite` as conta por partição. Os casos `-m s3` passaram no bucket do ambiente alvo em 2026-09-23, com SSE-KMS, e em 2026-09-25 com o deltalake 1.6.6, e passam no substituto local, o moto, onde o secret do DuckDB alcança o endpoint de `AWS_ENDPOINT_URL` pelo endereço por caminho e sem SSL. O secret do DuckDB guarda a credencial resolvida no `CREATE SECRET`, e a do contêiner expira em cerca de uma hora: no alvo, em 2026-09-25, o secret `credential_chain` com `REFRESH auto` (decisão do usuário de 2026-09-24) só foi renovado pelo `httpfs`, e o `delta_scan` falhou uma vez com a chave vencida, enquanto o delta-rs, o `S3FileSystem` e o `boto3` renovaram a credencial sozinhos ([`POC.md`](POC.md)). Desde a decisão do usuário de 2026-09-25, `duckdb_setup` cria o secret com a chave da credencial do `boto3` (`aws_credentials`), sem a extensão `aws`, e `renew_duckdb_secret` o recria quando a chave troca; o `deep_copy` copia os arquivos de cada partição e os registra desde a etapa 9, em 2026-09-24 (decisão do mesmo dia), e continua uma cópia interrompida; `Storage.copy` no S3 é a transferência gerenciada do `boto3` desde a bateria de 2026-09-24 às 12:38, em que o `CopyObject` único do `copy_file` foi abandonado pelo SDK da AWS num arquivo de 32 milhões de linhas ([`POC.md`](POC.md)). A leitura da renovação no alvo espera uma rodada de `probes/credentials.py` ([`OPEN_QUESTIONS.md`](OPEN_QUESTIONS.md)). |
| [4. `audit` e motor DuckDB](PLAN-STAGE-4.md) | Implementada em 2026-09-23 em pasta local: `serialize_db.audit` (as verificações do contrato e o texto delas nos dois dialetos, a contagem por `count(CASE WHEN ...)`, porque o Redshift não tem `FILTER` nos agregados), `serialize_db.engine` (o protocolo) e `serialize_db.engine.duckdb` (o motor, com o stream híbrido, o loader que cria a tabela no `close`, a auditoria e a exportação pelo registro do arquivo do `COPY`), e `SandboxError`. O texto da auditoria no Redshift rodou no ambiente alvo em 2026-09-23: às 18:52 reprovou no `is_valid_json` sobre `SUPER` e no `NaN` da varredura da tabela, que passou pelo `NOT IN`; às 22:56 e às 23:01, com `true` no JSON e a comparação estrita com os infinitos, rodou inteiro e somou certo, mas a negação da comparação não contou o `NaN`. A contagem dos não finitos passou a ser a dos não nulos menos a dos finitos, e as execuções de 2026-09-24 às 01:46 e às 01:49 contaram 2 dos 2, asserção desde então ([`POC.md`](POC.md)). Os limites da instância saem do ambiente na abertura, sem valor fixo: `threads` nas CPUs que o processo pode usar e `memory_limit` na metade da memória que ele ainda pode usar, por `environment_limits` sobre `serialize_db.resources`, com o cgroup v1 e v2 (instrução do usuário de 2026-09-24, no lugar do `memory_limit` padrão do DuckDB decidido em 2026-09-22). No ambiente alvo, em 2026-09-24, com 16 vCPUs e o cache de arquivos desligado, a materialização foi melhor com as CPUs do processo e pior com a metade e com o dobro, e a leitura do S3 1,4 vez mais rápida com o triplo: o padrão de `threads` são as CPUs do processo ([`POC.md`](POC.md)). As decisões que a implementação segue: o motor tem uma sessão por execução sob um `RLock` (decisão do usuário de 2026-09-22) e `new_session()`, um cursor criado sem o lock, para o que roda em paralelo. As decisões do usuário de 2026-09-23 fixaram o `stream` híbrido (os lotes em memória até 64 MiB e num arquivo Arrow IPC com LZ4 depois), o `interrupt()` no `close` do stream e no `cleanup`, o `loader` que confere o nome num cursor e cria a tabela no `close`, numa transação, o estilo `qmark`, o escopo das chaves da auditoria e a interface do motor (`query(statement_or_sql, params=None)` no lugar de `query` e `execute`, o `mode` resolvido em `Execution`, argumentos nomeados); o motor é a referência do stream, do loader e do cancelamento: os esboços de `tests/proof_of_concept/test_parallel.py` que os implementaram saíram, e os casos que só eles tinham estão em `tests/test_engine_duckdb.py` (decisão do usuário de 2026-09-23). O loader abandonado apaga o arquivo de transbordo quando a thread dele termina, como o stream. O texto da auditoria fica só para depuração, sem arquivos versionados (decisão do usuário de 2026-09-23). O `export_partition` registra o arquivo do `COPY`, sem o `rewrite` e sem a flag `export_mode` (decisão do usuário de 2026-09-24). No S3, o motor segura a credencial do `boto3` e recria o secret do S3 na entrada de cada sessão quando a chave dela trocou, sob um lock comum às sessões do banco, porque duas recriações ao mesmo tempo conflitam no catálogo do DuckDB (decisão do usuário de 2026-09-25); no substituto de chaves de 70 s, o motor leu em todas as rodadas, e o código anterior falhou a partir da expiração da primeira chave ([`POC.md`](POC.md)). Nenhuma decisão espera o usuário. |
| [5. Motor Redshift](PLAN-STAGE-5.md) | Implementada em 2026-09-24 sobre o substituto local: `serialize_db.engine.redshift` (`RedshiftConfig`, com `from_environment` das variáveis `SERIALIZE_DB_REDSHIFT_*`; `RedshiftEngine`, com a sessão única sob `RLock`, `session()`, `new_session()`, a reconexão uma vez por comando fora de transação, `ingest` por `COPY ... MANIFEST FILLRECORD` numa staging sem a coluna de partição e o `INSERT` com a lista de colunas, `published` na staging `_publicado_<versão>` carregada uma vez por execução e por versão, `stream` por `UNLOAD ... PARALLEL OFF` no `staging/` com o esquema do `row_desc` e a leitura dos arquivos numa thread, `query` pelo cursor montada por colunas, o `loader` que grava um Parquet no `staging/` e cria e carrega a tabela numa transação no `close`, a auditoria com a staging `_publicado_<versão>` carregada só quando a junção roda, e `export_partition` pelo registro dos arquivos do `UNLOAD` num prefixo novo por chamada, com a troca para `publish_partition` na partição com `Double` não finito e a releitura por `read_back` depois dela; `sandbox_prefix`, `schema_from_row_description`, `mask`), `file_from_footer` e `partition_values` em `serialize_db.delta`, `open_output_stream` em `serialize_db.storage`, `required_parameters` em `serialize_db.sql`, `redshift-connector==2.1.17`, no extra `redshift` até passar às dependências de execução em 2026-09-25 (decisões do usuário de 2026-09-24 e de 2026-09-25) e o motor `"redshift"` em `Execution` e no `serialize-db audit`. Os 21 casos de `tests/test_engine_redshift.py` passam, os 15 sem conexão sobre uma conexão de mentira e os 6 `redshift` sobre o substituto local. A primeira execução no ambiente alvo, em 2026-09-24 às 05:10 e às 05:12, passou cinco dos seis casos `redshift` (o `COPY ... MANIFEST`, o `UNLOAD`, o `loader`, a auditoria, a exportação pelo registro e pela troca, as duas sessões, os literais, o `load` de 10 linhas em 1,5 s) e reprovou `current_database()`, descrito com o tipo `name` (OID 19), que o mapa de tipos aceita como `string` desde então; o substituto imita o OID. A repetição das 13:01 e 13:03 passou os seis, e leu a relação inexistente respondida com o SQLSTATE `XX000` e a mensagem `Relation <nome> does not exist in the database.`, que o substituto imita desde então, e a coluna JSON do arquivo do `UNLOAD` como `VARCHAR` pelo `delta_scan` ([`POC.md`](POC.md)). O que a implementação mudou do plano está no arquivo da etapa, seção "A implementação". A releitura depois da troca e a staging com a versão no nome seguem as decisões do usuário de 2026-09-25 ([`PLAN-STAGE-5.md`](PLAN-STAGE-5.md)). Nenhuma decisão espera o usuário. |
| [6. Execução e linha de comando](PLAN-STAGE-6.md) | Implementada em 2026-09-23 em pasta local sobre o motor DuckDB: `serialize_db.execution` (`Database`, `Execution`, importados de `serialize_db`), `AuditFailed` e os subcomandos `serialize-db run` e `serialize-db audit`; o pool da publicação, em `serialize_db._pool` desde 2026-09-24, entrega uma tabela por worker livre, e a falha sobe com o resultado de cada tabela numa nota. Desde 2026-09-24, `Execution(..., redshift=RedshiftConfig(...))` constrói o motor `"redshift"` (sem a configuração, a das variáveis `SERIALIZE_DB_REDSHIFT_*`), e `serialize-db audit --engine redshift` audita a versão publicada no motor Redshift; `run.publish_redshift` e `serialize-db run --redshift` saíram na etapa 10, em 2026-09-25, e `Database` ganhou `open_delta` e `open_redshift`. As decisões que a implementação segue: `run.ingest` de mais de uma tabela carrega todas em paralelo, uma sessão a mais por tabela, e `serialize-db audit` perdeu `--write`. As decisões do usuário de 2026-09-23 fixaram `--metadata` no `serialize-db run`, `next_ids` só na chave sequencial e a regra da partição, `[0-9A-Za-z][0-9A-Za-z_.-]*`, no valor e no `execution_id`, e deixaram a barreira por tabela fora das etapas. Nenhuma decisão espera o usuário. |
| [7. Carga inicial](PLAN-STAGE-7.md) | Implementada em 2026-09-24 sobre a base fictícia: `serialize_db.load` (`discover_partitions`, `partition_query`, `initial_load` num motor DuckDB por chamada, com a conferência da partição antes da gravação, o `COPY ... RETURN_STATS` na ordem da `sort_key` e o registro por `register_files`; `load_report` com as `Double` só nos finitos; `load_order`; `entries_outside_the_model`), `serialize-db load` e o script `scripts/migrate_parquet_to_delta.py` fino sobre o pacote, sem a medição das variantes (decisões do usuário de 2026-09-24). Os 20 casos de `tests/test_load.py` e os 3 do script passam na pasta local. O script rodou no ambiente alvo, na versão anterior ao pacote, em 2026-09-23 às 23:05 e em 2026-09-24 às 01:53 (`cad_lancamentos` com o pico de 16.430 MB na partição 2026-03-31 sob `memory_limit` de 13,4 GiB, [`POC.md`](POC.md)); a carga pelo pacote rodou lá em 2026-09-24 às 14:16, na raiz `<raiz>/prod/<tabela>`, com as 12 tabelas conferidas, `cad_lancamentos` com o pico de 16.198 MB sob `memory_limit` de 14.030 MiB, e a auditoria com `--foreign-keys` leu os 989.852 órfãos conhecidos de 2026-01-31 ([`POC.md`](POC.md)); a repetição das 16:51 do mesmo dia, sobre a raiz recarregada, leu o mesmo, com `cad_lancamentos` em 19,9 s, 15,3 s, 31,8 s e 19,4 s e o pico de 16.355 MB sob `memory_limit` de 14.036 MiB ([`POC.md`](POC.md)); a de 2026-09-25 às 18:16, numa máquina de 8 vCPUs e 15.505 MB, carregou as 12 tabelas em 219,4 s, com `cad_lancamentos` no pico de 8.677 MB sob `memory_limit` de 6.227 MiB ([`POC.md`](POC.md)). Nenhuma decisão espera o usuário. |
| [8. Publicação para clientes](PLAN-STAGE-8.md) | Implementada em 2026-09-24 sobre o substituto local: `serialize_db.publication` (`control_ddl` sem `IF NOT EXISTS`, `create_publications_table`, `control_read`, `published_ddl` com a chave primária informativa, `publication_statements` com a linha de controle por último, `unpublication_statements`, `reconcile_published` pelas colunas de `svv_all_columns`, `publish_redshift` com uma conexão por tabela no pool e a transação que lê a linha de controle no início e a grava no fim, `unpublish_redshift`, `publication_status` e `PublicationStatus`), `PublicationError` em `serialize_db.errors` e `serialize-db publish` (`--init`, `--status`, `--unpublish`, `--tables`, `--max-workers`, `--execution-id` e, desde a etapa 10 em 2026-09-25, `--snapshot` ou `--channel`, um dos dois obrigatório para publicar; `Execution.publish_redshift` saiu no mesmo dia). A conexão é uma `RedshiftConfig` explícita (decisão do usuário de 2026-09-24). O `1023`, o `UPDATE` sem linha e a tabela publicada que outra primeira publicação criou saem como `ExecutionConflict`; o diff destrutivo da tabela publicada a despublica, e a publicação seguinte a recria. Os 13 casos de `tests/test_publication.py` passam, os 5 sem conexão sobre uma conexão de mentira e os 8 `redshift`, também `s3` e `local` porque os arquivos publicados saem do motor DuckDB com o `temp_directory` na pasta local, sobre o substituto local, entre eles a primeira publicação sobre os arquivos exportados pelo motor DuckDB, a publicação simultânea e, desde 2026-09-25, a linha de comando por canal e por snapshot com a volta a um snapshot anterior; a primeira bateria no ambiente alvo, em 2026-09-24 às 05:13, não rodou nenhum deles, porque a pasta de `SERIALIZE_DB_TEST_LOCAL_ROOT` não existia lá, e a das 13:05 e 13:08 passou os oito nas duas rodadas: o `COPY` do Redshift sobre os arquivos do `COPY` do DuckDB, a grafia de `svv_all_columns` sem diff, o `1023` como `ExecutionConflict` e a junção com `DS_DIST_ALL_NONE` ([`POC.md`](POC.md)); a publicação da base inteira rodou lá às 16:51 do mesmo dia, `--init`, `--tables cad_contas`, `--max-workers 4` com as 12 tabelas publicadas como `prod_<tabela>` (`cad_lancamentos` na versão 4 com 141.901.795 linhas) e `--status` sem partição pendente ([`POC.md`](POC.md)); a bateria de 2026-09-25 passou os oito duas vezes, entre eles a linha de comando por canal e por snapshot com a volta a um snapshot anterior ([`POC.md`](POC.md)). Cada tabela publicada vai ao log com as partições, o tempo e o pico de RSS do processo (decisão do usuário de 2026-09-24). Nenhuma decisão espera o usuário. |
| [9. Operação](PLAN-STAGE-9.md) | Implementada em 2026-09-24 na pasta local, fechando a ordem do trabalho: os subcomandos `serialize-db snapshot`, `vacuum`, `compact`, `archive`, `export` e `history`, `history` e `archive_snapshot` em `serialize_db.delta`, o `deep_copy` pela cópia dos arquivos de cada partição e o registro deles, com as estatísticas da própria versão, no lugar do `write_deltalake`, cuja memória cresce com a tabela fora do `memory_limit` (decisão do usuário de 2026-09-24), `snapshot` recusando o nome presente em `archived` e, desde a decisão do usuário de 2026-09-24, `delta.snapshot` o nome fora da regra da partição, que só a linha de comando e `Execution.snapshot` conferiam, e o runbook `docs/operacao.md`, incluído na página de `serialize_db.cli` (decisões do usuário de 2026-09-23: o lugar do runbook, a retenção de 400 dias em `docs/index.md`, a entrada arquivada na chave irmã `archived`). Os 6 casos de `tests/test_operation.py` passam. No ambiente alvo, em 2026-09-24, `history`, `snapshot` e `vacuum` rodaram sobre a raiz da carga, e `archive` morreu no `CopyObject` de um arquivo de `cad_lancamentos`, abandonado pelo SDK da AWS depois de 3 s sem resposta ([`POC.md`](POC.md)): `Storage.copy` passou à transferência gerenciada do `boto3`, `deep_copy` continua a cópia interrompida e `archive` deixou de pular a tabela presente no arquivo, que a repetição teria dado por arquivada pela metade; a bateria das 16:51 do mesmo dia, sobre a raiz recarregada, rodou `history`, `snapshot`, `vacuum` e o `archive` inteiro, os 21 arquivos das 12 tabelas pela transferência gerenciada e a entrada movida para `archived` ([`POC.md`](POC.md)); `compact`, `archive` e `export` imprimem por tabela o tempo e o pico de RSS do processo, e `deep_copy` registra o tempo de cada partição (decisão do usuário de 2026-09-24); a bateria das 23:25, sobre uma raiz nova em `prd`, repetiu a carga, a auditoria, `history`, `snapshot`, `vacuum`, `archive` (`cad_lancamentos` em 11,1 s) e a publicação (`cad_lancamentos` em 153,9 s), e rodou `export` pela primeira vez lá (8,8 s pelo registro, 17,3 s e 5.425 MB por `--mode rewrite`) ([`POC.md`](POC.md)); o `compact` rodou só sobre uma partição de um arquivo, que não commita, e a compactação de uma partição de vários arquivos lá espera ([`OPEN_QUESTIONS.md`](OPEN_QUESTIONS.md)); em 2026-09-25, numa máquina de 8 vCPUs, `export` levou 10,6 s pelo registro e 34,0 s com pico de 4.343 MB por `--mode rewrite`, e o `compact` saiu com a recusa do snapshot na versão atual ([`POC.md`](POC.md)). A decisão do usuário de 2026-09-25 sobre as colunas `Double` sem mínimo e máximo na compactação espera, por outra decisão do mesmo dia, a escolha da issue #85, que pode trocar a estatística ausente por mínimo e máximo largos: `delta.compact` ainda grava a estatística de toda coluna ([`PLAN-STAGE-9.md`](PLAN-STAGE-9.md), [`OPEN_QUESTIONS.md`](OPEN_QUESTIONS.md)). Nenhuma decisão espera o usuário. |
| [10. Acesso de leitura e canal do snapshot](PLAN-STAGE-10.md) | Implementada em 2026-09-25 na pasta local e no substituto local, sem decisão pendente: o canal `default` no arquivo de controle (`delta.set_channel`, `channel_snapshot`, `snapshot_versions`), movido só por `serialize-db channel --name default --snapshot <nome>`, e o canal reservado `current`, a versão atual; `serialize-db publish` só por `--snapshot` ou `--channel`, com a volta a um snapshot anterior por `version_diff` entre a menor e a maior das versões, e sem `run.publish_redshift` nem `serialize-db run --redshift`; e `serialize_db.reader`, com o leitor Delta (`db.open_delta()`: um DuckDB com uma view por tabela no snapshot do `default`, num snapshot nomeado, arquivado inclusive, ou na versão atual, e `materialize` de tabelas e partições) e o leitor Redshift (as tabelas `<ambiente>_<tabela>`, por `serialize_db.reader.open_redshift` ou por `db.open_redshift()`, com a conexão das variáveis `SERIALIZE_DB_REDSHIFT_*` sem `config` e o destino do `UNLOAD` do cliente, sem o qual só `query` roda). O leitor Delta não fechado apaga a pasta temporária por um `weakref.finalize`. Os 16 casos de `tests/test_reader.py` passam: 13 `local`, 2 sem conexão e o caso `redshift` que compara os dois leitores, no substituto; a poda pela view (`=`, `BETWEEN`, `IN` salteado) é medida pelo log `FileSystem` do DuckDB ([`POC.md`](POC.md)). No alvo, em 2026-09-25, o leitor Delta abriu as 12 views em 0,645 s, e o caso `redshift` do leitor e o da publicação por canal e por snapshot passaram nas suítes ([`POC.md`](POC.md)); a publicação da base inteira por canal, com o tempo e o pico de RSS por tabela, e o `UNLOAD` de um cliente só de leitura estão em [`OPEN_QUESTIONS.md`](OPEN_QUESTIONS.md). |

## O repositório

| Artefato | Estado |
| --- | --- |
| `src/serialize_db/` | O pacote das etapas 1 a 10: `errors.py` (`ContractError`, `SqlError`, as exceções da etapa 3, `ConflictError`, `ExecutionConflict`, `RegistrationRefused`, `SchemaDiffRefused` e `LogUnavailable`, `SandboxError`, `AuditFailed` e `PublicationError`), `audit.py` (as verificações do contrato, o texto por dialeto e o `AuditReport`), `engine/__init__.py` (o protocolo `Engine` e os auxiliares protegidos que os dois motores usam: `ARROW_ONLY`, `batches_of`, `checked_batches` e `take`), `engine/duckdb.py` (o motor DuckDB, com `environment_limits`, os limites lidos do ambiente na abertura), `engine/redshift.py` (o motor Redshift: `RedshiftConfig`, `RedshiftEngine`, `sandbox_prefix`, `schema_from_row_description`, `mask`, e os auxiliares protegidos da conexão, das credenciais e do texto do `COPY`, do `UNLOAD` e da staging que a publicação reaproveita), `publication.py` (a tabela de controle, a transação por tabela, a despublicação, a reconciliação das tabelas publicadas e o estado), `_pool.py` (privado: o pool de tarefas por tabela da execução, da publicação e do leitor), `resources.py` (as CPUs e a memória que o processo pode usar, com o cgroup v1 e v2, e `peak_rss_mb`, o pico de memória residente do processo que o script de migração, as rotinas de operação e a publicação imprimem por tabela), `execution.py` (`Database`, com `open_delta` e `open_redshift`, e `Execution`), `reader.py` (o acesso de leitura da etapa 10: `DeltaReader`, `RedshiftReader` e `open_redshift`), `load.py` (a carga inicial da base Parquet de origem: `discover_partitions`, `partition_query`, `initial_load`, `load_report`, `load_order`), `schema.py` (esquema Arrow e Delta, opções físicas, `sql_type`, o DDL como texto com todo identificador entre aspas e `temporary=True` para `CREATE TEMP TABLE`, o `cast` por lote, tabela e leitor, `check_models`, a regra da partição e os arquivos de esquema), `storage.py` (`Storage` sobre `pyarrow.fs`, com a escrita condicional, `create_text` e `write_text(if_match=...)`, pelo `boto3` no S3, `storage_options`, `duckdb_connect` e `prepare_environment`), `delta.py` (a criação, a publicação por partição, o registro de arquivos com as conferências e a releitura, a reconciliação, a reescrita, a diferença de versões, o manifesto, os snapshots e os canais deles (`set_channel`, `channel_snapshot`, `snapshot_versions`), o `vacuum`, a compactação, a cópia profunda e a exportação), `sql.py` (a cópia prefixada, `render`, `bind`, `referenced_tables`, os arquivos de texto SQL com o sentinela `{prefix}` e `bound_statement`, protegida, a cópia com os valores do cliente que os dois motores compilam), `_files.py` (privado: a gravação e o diff dos arquivos gerados que os dois módulos usam) e `cli.py` (`serialize-db schema`, `sql`, `run`, `audit`, `load`, `publish`, `snapshot`, `channel`, `vacuum`, `compact`, `archive`, `export` e `history`, com `main` exportado por `__init__.py` e o runbook `docs/operacao.md` incluído na docstring). O `__all__` de cada módulo é a interface pública e a documentação do `pdoc`. `pyproject.toml` fixa as dependências de execução, tudo o que a interface pública usa: boto3 1.43.102, deltalake 1.6.6, DuckDB 1.5.5, duckdb-engine 0.17.0, PyArrow 25.0.1, redshift-connector 2.1.17, o driver do motor Redshift, da publicação e do leitor Redshift, que `execution`, `reader` e `cli` importam só quando o motor entra, SQLAlchemy 2.0.54 e sqlalchemy-redshift 1.0.0; o grupo `dev` fixa pandas 3.0.6 e sqlglot 30.19.0 e traz pytest, boto3 e redshift-connector com limite inferior, o grupo `emulator` fixa moto 5.2.3, flask 3.1.3 e flask-cors 6.0.5, os do substituto local, fora do `dev` (decisão do usuário de 2026-09-23), e o grupo `docs` fixa pdoc 16.0.0. |
| `tests/` | Os testes do pacote, um módulo de teste por módulo, com as contagens de casos coletados: `test_schema.py` (65), `test_sql.py` (16), `test_storage.py` (13: sete sem gravar e três casos em cada raiz, local e S3), `test_delta.py` (74: 37 casos em cada raiz), `test_audit.py` (6, sem gravar), `test_engine_duckdb.py` (29, todos `local`, um deles também `s3`), `test_engine_redshift.py` (21: 15 sem conexão, sobre uma conexão de mentira, seis deles `local` porque gravam o arquivo de um `UNLOAD` de mentira ou um Delta na pasta local, e 6 `redshift`), `test_execution.py` (29, todos `local`), `test_operation.py` (8, todos `local`), `test_publication.py` (13: 5 sem conexão, três deles `local`, e 8 `redshift`, também `s3` e `local`, que publicam num ambiente `poc<id>` próprio), `test_reader.py` (16: 13 `local`, 2 sem conexão, sobre a conexão de mentira, e 1 `redshift`, também `s3` e `local`, que compara os dois leitores sobre uma tabela publicada), `test_client_model.py` (6), `test_reference_model.py` (4), `test_source_db_projetado.py` (14, todos `local`, com cada arquivo lido por `parquet_source.footer_columns`, a leitura do probe), `test_load.py` (20, todos `local`, sobre a base fictícia), `test_migrate_parquet_to_delta.py` (4, todos `local`), `test_resources.py` (6, todos `local`, sobre um `/proc` e um cgroup fabricados e um processo filho que toca 64 MiB), `test_probes.py` (72, as funções puras dos probes, sem rede salvo uma consulta DNS; os 18 casos que gravam o relatório ou arquivos fabricados são `local`), `test_conftest_redshift.py` (3) e `test_package.py` (2, sem gravar: cada submódulo público no `__all__` do pacote, que o `pdoc` documenta). `conftest.py` guarda a regra de autorização, as fixtures das três raízes (pasta local, bucket, Redshift, com a classe base `SessionRoot`), o relatório da sessão, que mascara as credenciais na impressão e no JSON, `describe_error`, o erro do `redshift_connector` numa linha para o relatório, `opened_partition_folders`, a leitura do log `FileSystem` do DuckDB, a chave `SERIALIZE_DB_TEST_EMULATOR`, que sobe o substituto local antes da coleta e dá a conexão dele a `connect_redshift`, e `redshift_config()` com a fixture `redshift_driver`, a configuração do motor Redshift das suítes e a conexão do substituto na porta `driver_connect`; `lancamentos_model.py` é o modelo das suítes do motor Redshift e da publicação (`Conta`, `Lancamento`, `Projetado`) com as linhas de uma partição no contrato (`entry_rows`, `account_rows`); `emulator.py` é o substituto local das suítes que só rodam no ambiente alvo: o moto num subprocesso no lugar do S3 e do STS, e uma conexão falsa do `redshift_connector` sobre um DuckDB em memória, que traduz o SQL do Redshift das suítes e imita as recusas e os comportamentos lidos no ambiente alvo (a contrabarra como escape nos literais, o `UNLOAD` vazio sem arquivo, `pg_last_unload_count()`, o `is_valid_json` que recusa `SUPER`, a relação inexistente como o `XX000` com a mensagem `Relation <nome> does not exist in the database.` do alvo e a existente como `42P07`, o conflito entre duas transações como o `1023` e `svv_all_columns` pelo DDL lembrado, na grafia do Redshift), com as falhas provocadas por `SERIALIZE_DB_TEST_EMULATOR_FAIL_SQL` e `SERIALIZE_DB_TEST_EMULATOR_NO_MANIFEST`; `source_db_projetado.py` é a base Parquet de origem fictícia, com a estrutura que o probe leu nas duas bases reais e `rel_contas_hierarquias` numa árvore de contas, e guarda as chaves do modelo de referência (`UNIQUE_KEYS`, `FOREIGN_KEYS`, `MODEL_NOT_NULL_DECLARED_NULLABLE`) que `test_reference_model.py` confere contra o modelo. O `pythonpath` do pytest, em `pyproject.toml`, põe `scripts/` e `probes/` no caminho de importação, e `test_migrate_parquet_to_delta.py` e `test_probes.py` importam os scripts pelo nome. Sem variável, 218 passam e 343 são pulados, e a sessão não grava arquivo algum (conferido num ambiente despido, com `HOME` e `TMPDIR` em pastas vazias, antes das etapas 5 e 8, cujos casos sem variável não tocam armazenamento); com a raiz local, 463 passam e 98 são pulados; com a raiz local e `SERIALIZE_DB_TEST_EMULATOR`, 560 passam e só o teste da Data API é pulado (Linux x86_64, 2026-09-25, deltalake 1.6.6). O substituto roda na máquina de quem desenvolve, antes do push, pelo grupo `emulator` (decisão do usuário de 2026-09-23). Nenhuma linha de `src/` e de `tests/`, fora do modelo de referência, passa de 100 caracteres. A esteira do GitHub roda `tests/` sem `tests/proof_of_concept/` e sem `test_probes.py`. |
| `tests/proof_of_concept/` | As provas de conceito e as suítes de estudo das bibliotecas externas, comentadas passo a passo: `poc_delta.py` com `test_local.py` (8) e `test_s3.py` (12), a camada Delta nos dois armazenamentos; `test_deltalake.py` (26), `test_duckdb.py` (27), `test_pyarrow.py` (9), `test_sqlalchemy.py` (17), `test_stdlib.py` (15), `test_concurrency.py` (8), `test_parallel.py` (3: a leitura e a ingestão paralelas em cursores do DuckDB cru, as escritas paralelas no Delta com o conflito, e as cargas e exportações de várias threads numa conexão), `test_redshift.py` (21, nove deles as leituras das decisões da etapa 5, do texto da auditoria sob `search_path` e, da etapa 8, do `ALTER COLUMN TYPE` e do `EXPLAIN` de um join no datashare) e `test_redshift_transactions.py` (9: duas transações simultâneas no esquema do datashare, uma delas com a sequência da publicação da etapa 8, e a staging temporária da publicação, cheia dentro da transação e antes do `BEGIN`). As três rodaram no ambiente alvo em 2026-09-23 às 18:48, quando a Redshift reprovou nas duas execuções só o `stream` com literais, na contrabarra, e às 22:53 (S3, 445 aprovados) e às 22:56 e 23:01 (Redshift, 30 aprovados em cada), com os casos novos, e em 2026-09-24 às 01:43 (S3, 454 aprovados) e às 01:46 e 01:49 (Redshift, 30 em cada), numa máquina de 16 vCPUs ([`POC.md`](POC.md)). A igualdade dos três caminhos do `stream`, o `UNLOAD` vazio, a tabela temporária e a recusa do `ALTER COLUMN ... TYPE` viraram asserção, e a contagem dos não finitos e a linha do `NaN` também, depois das execuções de 2026-09-24 às 01:46 e às 01:49 (a linha só no ambiente alvo). As três passam no substituto local de `tests/emulator.py`, o moto no lugar do S3 e o DuckDB no do Redshift, que imita também a recusa do `ALTER COLUMN ... TYPE` (41 casos aprovados e a Data API pulada, 2026-09-24). As suítes de estudo guardam só os fatos das bibliotecas externas, e o código do pacote que elas usam é o de `src/`, sem cópias (decisão do usuário de 2026-09-23): em 2026-09-24 o `unload_text` e o guarda dos `bindparam` de `test_redshift.py` e o `sandbox_prefix` de `test_stdlib.py` passaram a vir do pacote; elas ensinam as abordagens das decisões de 2026-09-21 e 2026-09-22: os comportamentos do compilador que dão forma ao `render`, o DDL do Redshift sem regra `@compiles`, o esquema Delta sem `parquet.field.id`, o texto medido em bytes, as estatísticas dos tipos de `stat_converter`, a `version_diff` pelo `dataChange` do log, a carga com o `double` mantido e as partições no lugar dos meses. |
| `tests/reference_model/` | O modelo de referência: o modelo SQLAlchemy da base original em Parquet particionado, que fica como está (decisão do usuário de 2026-09-21); `tests/lib_base_contabil.py` e `tests/lib_base_gerencial.py` fazem os nomes que ele importa apontarem para os arquivos dele. Ele bate com as duas leituras da base de origem, exceto a nulidade de sete colunas de `cad_contratos`, e `check_models` lista os seus defeitos. |
| `tests/client_model/` | O modelo cliente: a cópia corrigida do modelo de referência, o modelo de dados que o código cliente apresenta à biblioteca (decisão do usuário de 2026-09-21), com `Table.info["serialize_db"]` nas quatro tabelas particionadas e `UniqueConstraint` atrás das duas chaves estrangeiras compostas; `statements.py` com os quatro statements Core do pipeline fictício; `schema/` com os 36 arquivos de esquema e `sql/` com os oito de texto SQL. |
| `docs/` | A documentação do pacote para o `pdoc`: `docs/index.md`, a página principal, com o funcionamento geral, o tutorial, a retenção dos arquivos removidos e a tabela de mapeamento de tipos, incluída pela docstring de `src/serialize_db/__init__.py`; `.github/workflows/docs.yml` a publica em <https://felipenoris.github.io/serialize-db/>. O site tem uma página por módulo público, `serialize_db.engine.duckdb`, `serialize_db.engine.redshift`, `serialize_db.publication` e `serialize_db.resources` incluídos; o tutorial tem as seções do sandbox Redshift, da publicação, da carga inicial e da leitura com o modelo, que lista os leitores de fora do pacote que leem as tabelas com filtro certo e os que erram; `docs/operacao.md` é o runbook da etapa 9, seguido das opções de cada subcomando de `serialize-db`, incluído pela docstring de `src/serialize_db/cli.py`. A docstring de cada função e método que o site mostra documenta cada argumento, o retorno e as exceções nos campos `:param`, `:return:` e `:raises`, e cada campo de dataclass e atributo público tem a sua (decisão do usuário de 2026-09-24). |
| `plan/` | O plano (`PLAN.md` e `PLAN-STAGE-0.md` a `PLAN-STAGE-10.md`), este documento, `POC.md`, `OPEN_QUESTIONS.md`, os documentos de estudo (`parquet.md`, `duckdb.md`, `redshift.md`, `sqlalchemy.md`, `schema.md`, `delta.md`, `guia.md`, `estrategia.md`, `serialize-db.md`) e `readings/`, os relatórios de probe e de suíte que uma etapa pendente ainda consulta, indexados por `plan/readings/README.md`: os de 2026-09-24 e os probes de 2026-09-23 que a bateria seguinte não repetiu, mascarados, e o da base de produção de 2026-09-21, que a etapa 7 e a base fictícia consultam. |
| `examples/` | Os scripts que o usuário executou no ambiente alvo, guardados como rodaram, salvo os identificadores do ambiente mascarados no caminho do bucket: `redshift_native.py`, `redshift_data_api.py` e `redshift_copy_unload.py` (2026-09-20) e `redshift_manifest.py` (2026-09-21); `examples/README.md` diz o que cada um fixa. O caderno `examples/notebooks/examples-sqlalchemy.ipynb` usa o grupo `interactive` de `pyproject.toml`. |
| `probes/` | As leituras do ambiente, só de leitura, sobre `probelib.py`: `space.py`, `bucket.py`, `diagnose_aws.py`, `redshift.py`, `catalog.py`, `parquet_source.py`, `duckdb_threads.py` e `credentials.py`; `probes/README.md` descreve cada checagem, e os relatórios saem em `probes/output/`, fora do git. Os cinco primeiros rodaram no ambiente alvo em 2026-09-21 e duas vezes em 2026-09-23, a partir da `main`, às 19:18 com `RS-19` pelo critério novo e a leitura `RS-8`; às 22:49 o `redshift.py` parou a seção da sessão num `count(*)` sem linha, e agora lê a falta da linha como leitura (`first_value`, `count_text`); em 2026-09-24 às 01:41 ele leu a seção inteira, com 25 erros de carga em 30 dias em `sys_load_error_detail`. `parquet_source.py` leu a base de desenvolvimento em 2026-09-20 e a de produção em 2026-09-21. `duckdb_threads.py` rodou no ambiente alvo em 2026-09-23 às 23:21, com o cache de arquivos externos do DuckDB servindo da memória as repetições seguintes à primeira, e em 2026-09-24 às 02:02, com o cache desligado em cada configuração, a metade das CPUs do processo (pedido do usuário de 2026-09-24), a razão sobre o padrão do motor e as threads por núcleo físico: numa máquina de 16 vCPUs, o padrão foi o melhor da materialização ([`POC.md`](POC.md)). `credentials.py`, escrito em 2026-09-25 para a pergunta das credenciais de uma hora ([`OPEN_QUESTIONS.md`](OPEN_QUESTIONS.md)), rodou num substituto com chaves de 70 s e no alvo, em 2026-09-25 de 18:33 a 19:36, e nos dois o `delta_scan` falhou com a chave vencida do secret, que o `read_parquet` renovou; no alvo, o delta-rs, o `S3FileSystem`, o `boto3` e a conexão Redshift seguiram lendo ([`POC.md`](POC.md)). Desde a renovação do secret decidida pelo usuário em 2026-09-25, a sonda lê o DuckDB pelo `DuckDBEngine`, e no substituto o motor leu em todas as rodadas. Na mesma bateria, os cinco primeiros rodaram de novo, e `duckdb_threads.py` parou em `DT-1`, com a raiz passada sem a pasta `prd` do ambiente. |
| `scripts/` | `migrate_parquet_to_delta.py`, a ferramenta de operação da carga inicial da [etapa 7](PLAN-STAGE-7.md), fina sobre `serialize_db.load` desde 2026-09-24: por tabela, na ordem de `load_order`, `initial_load` partição por partição, com as linhas, o tempo e o pico de memória do processo (`serialize_db.resources.peak_rss_mb`) impressos, `load_report` e `entries_outside_the_model`; `--report` grava o JSON com a máquina, as versões, os limites do DuckDB lidos do ambiente e os argumentos, regravado depois de cada partição gravada com a tabela da vez em `in_progress`; `--environment` dá a pasta das tabelas sob a raiz, com `SERIALIZE_DB_ENVIRONMENT` vazia lida como ausente, como nos subcomandos de `serialize-db`; pasta local ou `s3://` na origem e na raiz. A medição das variantes saiu com a decisão do usuário de 2026-09-24. O `README.md` traz os comandos e `tests/test_migrate_parquet_to_delta.py` os 4 casos. |
| `.github/` | `workflows/tests.yml`, os testes do pacote a cada push e pull request, precedidos da importação de cada módulo do pacote instalado sem o grupo `dev` (`uv sync --no-dev`), com as extensões `delta` e `httpfs` do DuckDB instaladas em `.duckdb/`, e `workflows/docs.yml`, a documentação no GitHub Pages a cada push na `main`. |
| `prepare_offline.sh` | Deixa a pasta autossuficiente para o destino sem internet (`.python/`, `.venv/` com todos os grupos e os extras, `.duckdb/`); o cabeçalho é o procedimento. Rodou de novo em 2026-09-22, quando os dialetos entraram nas dependências de execução, e a pasta preparada rodou a migração adiantada num ambiente despido em 2026-09-21; a pasta preparada de novo para a bateria de 2026-09-25 levou ao alvo o deltalake 1.6.6, o boto3 1.43.102 e o `redshift_connector` 2.1.17, então do extra `redshift` (`SP-9`). |
| `CLAUDE.md` e `.claude/memory/` | A memória do assistente, em inglês: `CLAUDE.md`, carregado em toda sessão, com o índice do repositório e da memória, as regras de trabalho, as convenções de nomes e onde o trabalho está; `.claude/memory/<tema>.md`, lido sob demanda, com os fatos por tema, as decisões do usuário (`decisions`) e as lições (`lessons`). |
