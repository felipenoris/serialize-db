# Estado da implementação

Este documento registra onde a implementação do plano está, e cada unidade de trabalho o atualiza. O
plano, com as decisões, as regras e as etapas, está em [`PLAN.md`](PLAN.md); o que as provas de
conceito e as leituras do ambiente mostraram, em [`POC.md`](POC.md); as pendências e as decisões em
aberto, em [`OPEN_QUESTIONS.md`](OPEN_QUESTIONS.md). O estado descrito aqui é o de 2026-09-20.

## Situação de cada etapa

Nenhum módulo do pacote existe: `src/serialize_db/` tem só o `main` de exemplo do `uv init`, e
`tests/` ainda não tem teste do pacote. A ordem do trabalho está em [`PLAN.md`](PLAN.md).

| Etapa | Situação |
| --- | --- |
| [0. Prova de conceito na AWS](PLAN-STAGE-0.md) | Os itens de S3 estão verificados. No Redshift, `probes/redshift.py` rodou no ambiente alvo em 2026-09-20 e fixou a conexão, o banco do esquema e o que falta; os itens que escrevem esperam um papel IAM associado ao namespace. |
| [1. `schema`](PLAN-STAGE-1.md) | Não começou; abre a ordem do trabalho, em pasta local. |
| [2. `sql`](PLAN-STAGE-2.md) | Não começou; roda em pasta local, junto com a etapa 1. |
| [3. `storage` e `delta`](PLAN-STAGE-3.md) | Não começou; os casos `-m s3` exigem o bucket. |
| [4. `audit` e motor DuckDB](PLAN-STAGE-4.md) | Não começou; roda em pasta local. |
| [5. Motor Redshift](PLAN-STAGE-5.md) | Não começou; a conexão é a credencial temporária do workgroup, e o esquema vem de um banco de datashare. |
| [6. Execução e linha de comando](PLAN-STAGE-6.md) | Não começou; roda em pasta local. |
| [7. Carga inicial](PLAN-STAGE-7.md) | Não começou; a base de origem foi lida em 2026-09-20 ([`POC.md`](POC.md)), e `tests/source_db_projetado.py` a reproduz para o teste da carga. |
| [8. Publicação para clientes](PLAN-STAGE-8.md) | Não começou; publica no esquema do datashare, com nome em três partes e `COPY ... COMPUPDATE OFF`. |
| [9. Operação](PLAN-STAGE-9.md) | Não começou; fecha a ordem do trabalho. |

## O repositório

| Artefato | Estado |
| --- | --- |
| `examples/` | Os dois scripts de conexão com o Redshift que o usuário executou no ambiente alvo em 2026-09-20, guardados como foram executados: `redshift_native.py` (protocolo nativo com credencial temporária do workgroup serverless, o caminho da biblioteca) e `redshift_data_api.py` (Data API por HTTPS, assíncrona, fora da biblioteca por decisão do usuário). `examples/README.md` diz o que cada um fixa: região `sa-east-1`, workgroup `controladoria-wg`, banco `dev` na conexão e esquema `sbx_aco_decon` no banco de datashare `datalake_rw_shared`, citado por nome em três partes. |
| `docs/readings/` | Os relatórios de probe que sustentam uma afirmação de `POC.md`, guardados como saíram: as duas leituras do Redshift no ambiente alvo em 2026-09-20 (20:37 e 20:43), indexadas por `docs/readings/README.md`. `probes/output/` continua fora do git. |
| `docs/` | Completa: `parquet.md`, `duckdb.md`, `redshift.md`, `sqlalchemy.md`, `schema.md`, `delta.md`, `guia.md`, `estrategia.md`, `serialize-db.md`, o plano (`PLAN.md` e `PLAN-STAGE-0.md` a `PLAN-STAGE-9.md`), este documento, `POC.md` e `OPEN_QUESTIONS.md`. Todo bloco Python dos documentos rodou com as versões fixadas em `pyproject.toml`; os comandos do Redshift foram compilados, não executados. |
| `tests/model/` | O modelo de referência: os modelos do pipeline (`model_base_contabil.py`, `model_base_gerencial.py` e `model_db_projetado.py`), movidos do pacote para os testes em 2026-09-20. Ele faz o papel da biblioteca cliente: os testes o entregam à API do pacote como um pipeline entregaria os seus modelos, e o pacote não contém modelo algum. Dois não importam (`from lib_base_contabil import Base` e `from lib_base_gerencial import Base`, módulos que o repositório não tem). Os modelos deixam o `autoincrement` padrão nas chaves inteiras (o `duckdb_engine` emite `SERIAL`, que o DuckDB rejeita), as declaram `Integer` onde o contrato passa a `BigInteger`, declaram chaves estrangeiras `DEFERRABLE INITIALLY DEFERRED` (o DuckDB descarta a cláusula, o Redshift não a tem) e não têm a coluna de partição `data_str`, comentários nem `Table.info["serialize_db"]`; as colunas numéricas ficam `Double` por decisão de 2026-09-20. A [etapa 1](PLAN-STAGE-1.md) os corrige. |
| `src/serialize_db/__init__.py` | Só o `main` de exemplo do `uv init`; o pacote não tem outro módulo. `pyproject.toml` não declara dependência de execução; o grupo `dev` fixa pytest, deltalake 1.6.4, DuckDB 1.5.5, PyArrow 25.0.1, boto3, redshift-connector, SQLAlchemy 2.0.54, duckdb-engine 0.17.0, sqlalchemy-redshift 1.0.0 e pandas 3.0.6. |
| `tests/` | `conftest.py` com a regra de autorização e as fixtures dos três alvos (pasta local, bucket, Redshift); `source_db_projetado.py`, a base Parquet de origem fictícia com a estrutura que o probe leu na base de desenvolvimento em 2026-09-20 (as 14 tabelas com as colunas, os tipos e a nulidade lidos, as partições Hive por `data_str` e `data_base_str` com o valor só no caminho, os chunks numerados sem zeros à esquerda, um row group por arquivo, `INT96` sem estatística, a chave `pandas`, o `schema.json` real da biblioteca anterior na raiz), consistente com o modelo de referência (chaves únicas, chaves estrangeiras satisfeitas, as quatro datas em todas as tabelas particionadas, a relação N×N de `rel_contrato_operacao` com `fator_rateio` somando 1 por operação), o material do teste da carga inicial, e `test_source_db_projetado.py`, que a grava na pasta temporária do pytest e a confere contra a seção 3 do relatório transcrita, as chaves do modelo e o controle de esquema; `test_probes.py` testa as funções puras dos probes com respostas fabricadas, sem rede, inclusive as de `parquet_source.py`, que recebem colunas e rodapés fabricados e não abrem arquivo algum. `tests/proof_of_concept/` com a prova de conceito da camada Delta nos dois armazenamentos (`poc_delta.py`, `test_local.py`, `test_s3.py`), as suítes de estudo das bibliotecas externas e da stdlib (`test_sqlalchemy.py`, `test_duckdb.py`, `test_pyarrow.py`, `test_deltalake.py`, `test_stdlib.py`, `test_concurrency.py`, `test_parallel.py`), comentadas passo a passo por serem o material de aprendizado de quem dará manutenção (`test_pyarrow.py`, `test_duckdb.py` e `test_parallel.py` cobrem a fronteira por lotes: o `cast` do lote e as conversões sem cópia, o leitor num cursor próprio, a consulta em streaming com a memória medida em subprocesso, o `INSERT` sobre um leitor de gerador com a leitura antecipada e os lotes que o leitor não confere, e os esboços `BatchStream` e `Loader` com o pipeline de três estágios), e `test_redshift.py`, os itens Redshift da [etapa 0](PLAN-STAGE-0.md), que conecta pelo caminho de `examples/redshift_native.py`, cita as tabelas por nome em três partes, põe `COMPUPDATE OFF` em cada `COPY` e ganhou um teste do banco do esquema e um do ciclo da Data API; ainda não executado. Cada arquivo de etapa lista as provas de conceito que exercitam as suas APIs. Sem variável, 110 testes passam e 60 são pulados; com a raiz local, 148 passam e 22 são pulados (2026-09-20, macOS). No espaço, em 2026-09-20, uma sessão com a raiz local e a raiz S3 gravou o JSON de `SERIALIZE_DB_TEST_REPORT` com as medições das duas raízes, sem a contagem por resultado nem o registro da limpeza, que o relatório passou a ter (`session.`, `local.cleanup`, `s3.cleanup`); a execução das 04:52 UTC, após as correções de 2026-09-20, registrou 104 testes passados e 7 pulados em 35 s e a limpeza das duas raízes; sem variável, 63 passam e 48 são pulados. Depois da revisão do Redshift de 2026-09-20: sem variável, 102 passam e 59 são pulados; com a raiz local, 140 passam e 21 são pulados (macOS). |
| `probes/` | Leituras do ambiente, só de leitura: `space.py`, `bucket.py`, `diagnose_aws.py`, `redshift.py`, `catalog.py` e `parquet_source.py` sobre `probelib.py`, com `redshift.py` revisto em 2026-09-20 a partir dos exemplos do ambiente alvo (`RS-15`, a credencial temporária do workgroup; `RS-16`, o banco do esquema do projeto, local ou de datashare; `RS-17`, os três requisitos da escrita num datashare; `RS-10` passou a rodar o ciclo completo da Data API, e `RS-5` e `RS-8` ganharam o ramo do esquema compartilhado) e corrigido pela primeira execução real no mesmo dia (o isolamento `UNKNOWN` virou leitura ausente, um tempo limite de leitura deixou de derrubar as checagens seguintes com erro obscuro, `RS-6` separou o papel padrão do apenas associado, e as cinco chamadas cuja falha é leitura saíram do código de saída por `Report.call(expected=True)`), com o resultado em `probes/output/` para colar na conversa. `parquet_source.py` lê a estrutura da base Parquet de origem da carga inicial ([etapa 7](PLAN-STAGE-7.md)) e rodou contra a base de desenvolvimento em 2026-09-20 (14 tabelas, 205 arquivos, 3,76 GB, 187 milhões de linhas, nenhuma checagem reprovada; a leitura está em [`POC.md`](POC.md)); as suas funções puras são exercitadas por `tests/test_probes.py` e por duas bases fabricadas em 2026-09-20 (uma com partição Hive, pastas sem nome e coluna a mais; outra com coluna de partição dentro do arquivo, profundidade misturada, tipo trocado e arquivo sem linha), que as checagens `PQ-3` a `PQ-6` e `PQ-9` apontaram. `probelib.duckdb_proxy` separa o endereço do proxy das credenciais para `space.py` e para o subprocesso DuckDB de `diagnose_aws.py`, e `hide_credentials` tira o usuário e a senha de toda variável de proxy do relatório, que antes saía com a senha embutida (2026-09-20). Quatro execuções no laboratório em 2026-09-20, sem Redshift, corrigiram a leitura das conexões, as leituras de rede, o rótulo dos IPs públicos, a montagem de `~/shared` e o Object Lock, acrescentaram por tabela Delta os arquivos, commits e último objeto, as sessões da suíte S3 e as versões não correntes sob a raiz, e a quarta isolou o 403 do delta-rs (`NO_PROXY` vazia) e fez `diagnose_aws.py` rodar o delta-rs como encontrado e como a suíte; o ambiente de destino ainda não foi lido. |
| `prepare_offline.sh` | Deixa a pasta autossuficiente para o destino sem internet (`.python/`, `.venv/`, `.duckdb/`); verificado extraindo o pacote em outro caminho e rodando a suíte local com proxies mortos. O cabeçalho é o procedimento: como rodar, as variáveis do proxy e os comandos de empacotar e extrair, que saíram do `README.md`. Desde 2026-09-20 o bloco das extensões configura o proxy do DuckDB por `probelib.duckdb_proxy`, a mesma função dos probes: o DuckDB recusa o endereço com as credenciais embutidas ([`POC.md`](POC.md)). |
