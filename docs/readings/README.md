# `docs/readings/`: os relatórios dos probes que viraram registro

Cada arquivo aqui é a saída de uma execução de `probes/<script>.py` num ambiente real, ou o JSON
de `SERIALIZE_DB_TEST_REPORT` de uma sessão de suíte, guardados como saíram. A pasta `probes/output/`
fica fora do git e recebe toda execução; esta guarda as que sustentam uma afirmação de
[`../POC.md`](../POC.md), que é onde a leitura é interpretada. O nome é
`<script>-<AAAA-MM-DD>[-<HHMM>].txt` para um probe e `<suíte>-suite-<AAAA-MM-DD>-<HHMM>.json` para
uma suíte, com a hora em UTC.

| Arquivo | Ambiente e hora | O que mostrou |
| --- | --- | --- |
| [`redshift-2026-09-20-2037.txt`](redshift-2026-09-20-2037.txt) | Ambiente alvo, 20:37 UTC | A primeira leitura do Redshift: workgroup `controladoria-wg`, endpoints VPC para as três APIs, credencial temporária, `sbx_aco_decon` no banco de datashare, nenhum papel IAM no namespace, `CREATE` negado no banco da conexão. Expôs quatro defeitos do probe, corrigidos no mesmo dia. |
| [`redshift-2026-09-20-2043.txt`](redshift-2026-09-20-2043.txt) | Ambiente alvo, 20:43 UTC, com `SERIALIZE_DB_TEST_S3_ROOT` | A repetição com a raiz S3, transcrita a partir da seção 1. `sys_load_error_detail` respondeu `0` em 1,5 s e `svv_external_schemas` também: o tempo limite da leitura anterior era o defeito do probe, não visão negada. O resto repetiu. |
| [`redshift-suite-2026-09-21-1050.json`](redshift-suite-2026-09-21-1050.json) | Ambiente alvo, 10:50 UTC, `pytest -m redshift` | A primeira execução da suíte Redshift: passou o `fetchmany` em fatias; o primeiro teste reprovou com `has_schema_privilege` em `false` depois do `USE`, e os outros nove, com os onze `DROP` da limpeza, em `25P02`, porque a sessão correu numa transação aberta antes do `USE` e abortada pela visão `stv_slices` negada. O JSON ainda não levava a mensagem das falhas; a saída do terminal, colada na conversa, as deu. |
| [`redshift-suite-2026-09-21-1128.json`](redshift-suite-2026-09-21-1128.json) | Ambiente alvo, 11:28 UTC, `pytest -m redshift` com o `conftest` corrigido | Sete passaram e quatro reprovaram: `COPY ... MANIFEST` com `File not found` pela barra dobrada na URL do manifesto (`…/operacoes//mes=…`), e `information_schema.columns` vazia para o esquema do datashare depois do `USE`. O `UNLOAD ... PARTITION BY ... MANIFEST VERBOSE` passou e o `schema.elements` do manifesto lista a coluna de partição; `has_schema_privilege` repetiu `false`; a Data API respondeu em 444 ms. |
| [`redshift-suite-2026-09-21-1208.json`](redshift-suite-2026-09-21-1208.json) | Ambiente alvo, 12:08 UTC, `pytest -m redshift` com a suíte corrigida pela segunda execução | Dez passaram e um reprovou: a contagem repetida depois do terceiro `TRUNCATE` de `test_copy_column_list_and_fillrecord` recebeu `34510`, `Concurrent DDL committed ... between Prepare and Execute`, do cache de prepared statements do `redshift_connector`. O `COPY ... MANIFEST` carregou `DECIMAL` em `INT64` e `timestamp_ntz`; a lista de colunas carregou 100 linhas com a coluna nova nula; o posicional reprovou com `Unmatched number of columns`; o `VARCHAR` excedido abortou com o motivo em `sys_load_error_detail`; `SUPER` recebeu 80.901 bytes por `INSERT` e recusou o `COPY` de Parquet sem `SERIALIZETOJSON`; o `UNLOAD` nomeou `mes=.../0064_part_00.parquet` e recusou o mesmo prefixo e o prefixo pai; dois `COPY` em 4,5 s e dois `UNLOAD` em 1,9 s em paralelo. |
| [`redshift-suite-2026-09-21-1210.json`](redshift-suite-2026-09-21-1210.json) | Ambiente alvo, 12:10 UTC, a repetição | O mesmo resultado, teste a teste: a mesma reprovação e as mesmas leituras; o `UNLOAD` nomeou `0000_part_00.parquet`, a Data API respondeu em 177 ms, os `COPY` paralelos em 3,6 s e os `UNLOAD` em 1,5 s. A repetição fixou o `34510` como permanente. |
